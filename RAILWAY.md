# 🚂 Деплой на Railway

## Быстрый старт

1. **Форкните репозиторий на GitHub**

2. **Создайте новый проект на Railway:**
   - Откройте https://railway.app
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ветку: `claude/telegram-bot-aiogram-RKgac`

3. **Добавьте PostgreSQL:**
   - В проекте нажмите "+ New"
   - Выберите "Database" → "Add PostgreSQL"
   - Railway автоматически создаст `DATABASE_URL`

4. **Добавьте Redis:**
   - Нажмите "+ New"
   - Выберите "Database" → "Add Redis"
   - Railway автоматически создаст `REDIS_URL`

5. **Настройте переменные окружения:**

Перейдите в Settings → Variables и добавьте:

### Обязательные переменные:

```
BOT_TOKEN=ваш_токен_от_BotFather
PAYMENT_TOKEN=ваш_payment_token_от_BotFather
CHANNEL_ID=-1001234567890
```

### Опциональные переменные:

```
# Документы
OFFER_DOCUMENT_URL=https://example.com/offer.pdf
PRIVACY_DOCUMENT_URL=https://example.com/privacy.pdf
CONSENT_DOCUMENT_URL=https://example.com/consent.pdf

# Тарифы (по умолчанию уже установлены)
TARIFF_30_DAYS=30
TARIFF_30_PRICE=199000
TARIFF_90_DAYS=90
TARIFF_90_PRICE=477000
TARIFF_365_DAYS=365
TARIFF_365_PRICE=1590000

# Таймер напоминания (по умолчанию 600 секунд = 10 минут)
REMINDER_DELAY_SECONDS=600

# Фото грустного кота
SAD_CAT_PHOTO_URL=https://i.imgur.com/sad_cat.jpg
```

## Автоматическая настройка БД

Railway автоматически создаст следующие переменные:

- `DATABASE_URL` - PostgreSQL (используется автоматически)
- `REDIS_URL` - Redis (используется автоматически)

Бот автоматически распарсит `DATABASE_URL` в:
- `DB_HOST`
- `DB_PORT`
- `DB_USER`
- `DB_PASS`
- `DB_NAME`

## Процесс деплоя

Railway автоматически:

1. ✅ Установит зависимости из `requirements.txt`
2. ✅ Запустит миграции БД: `alembic upgrade head`
3. ✅ Запустит бота: `python -m bot`

## Проверка работы

После деплоя:

1. Откройте вкладку "Deployments"
2. Проверьте логи - должно быть:
   ```
   INFO - Bot started
   INFO - Scheduler started
   INFO - Starting polling...
   ```

3. Откройте бота в Telegram: `/start`

## Переменные окружения Railway

Railway автоматически предоставляет:

- `PORT` - порт для веб-сервера (если используете webhook)
- `RAILWAY_ENVIRONMENT` - окружение (production/staging)
- `RAILWAY_PUBLIC_DOMAIN` - публичный домен (для webhook)

## Troubleshooting

### Бот не запускается

1. Проверьте логи в Railway
2. Убедитесь что `BOT_TOKEN` правильный
3. Проверьте что PostgreSQL и Redis подключены

### Миграции не применились

Railway автоматически запускает `alembic upgrade head` перед стартом бота (команда `release` в Procfile).

Если нужно запустить вручную:
1. Откройте Terminal в Railway
2. Выполните: `alembic upgrade head`

### База данных не создается

Railway автоматически создает БД при добавлении PostgreSQL сервиса.

Структура таблиц создается через Alembic миграции.

## Мониторинг

Railway предоставляет:
- Логи в реальном времени
- Метрики CPU/Memory
- Автоматические рестарты при падении

## Стоимость

- PostgreSQL: ~$5/месяц
- Redis: ~$5/месяц
- Compute: $5/месяц за 500 часов

**Итого:** ~$15/месяц

## Масштабирование

Для масштабирования:
1. Settings → Resources
2. Увеличьте RAM/CPU по необходимости

## Webhook (опционально)

Если хотите использовать webhook вместо polling:

```
USE_WEBHOOK=True
WEBHOOK_PATH=/webhook
```

Railway автоматически предоставит `RAILWAY_PUBLIC_DOMAIN`.

Бот автоматически настроит webhook на:
`https://{RAILWAY_PUBLIC_DOMAIN}/webhook`

## GitHub Actions (автодеплой)

Railway автоматически деплоит при push в ветку `claude/telegram-bot-aiogram-RKgac`.

Отключить автодеплой: Settings → GitHub → Disable Auto Deploy
